int min_value = 0;
        int max_value = 0;
                         
        for (int i = 0 ;i < arr.length - 1; i++)
        {
            
            min_value += arr[i];
            max_value += arr[j];
            j--;
        }
         Console.WriteLine( min_value + " " + max_value);
